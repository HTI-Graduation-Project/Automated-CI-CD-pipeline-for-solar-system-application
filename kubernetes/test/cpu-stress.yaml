apiVersion: batch/v1
kind: Job
metadata:
  name: cpu-stress
  namespace: production
spec:
  template:
    spec:
      containers:
      - name: stress
        image: polinux/stress
        args: ["--cpu", "2", "--timeout", "60s"]
      restartPolicy: Never
